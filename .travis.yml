# Config

language: cpp
sudo: false
dist: trusty
cache: bundler
notifications:
  email: false

# Build matrix (2x compiler, 1x confopts)
compiler:
  - gcc
  - clang

# Extend build matrix
matrix:
  include:
    - os: linux
      compiler: gcc
    - os: osx
      compiler: clang

script:
  - make
